<UserControl x:Class="I2MS2.UserControls.regionFlagTextControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="140" Foreground="{StaticResource _brushButtonNormalText}">
    <Grid>
        <!--<Rectangle Margin="0" RadiusX="5" RadiusY="5" Stroke="{DynamicResource CheckBoxBrush}"/>-->

        <!--<Grid.Effect>
            <DropShadowEffect/>
        </Grid.Effect>-->
        <Grid Margin="0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <!--<Ellipse Fill="#FFBDBBBA" StrokeThickness="0.5" Margin="0,17,0,0"/>-->
            <Rectangle Fill="{StaticResource _brushDarkGray2}" RadiusY="4" RadiusX="4" />
            <TextBlock x:Name="region1_text" Style="{DynamicResource I2MS_TextBlockStyle}" FontSize="15" FontFamily="Myriad Pro Black" Text="Text" Foreground="{StaticResource _brushButtonNormalText}" Margin="5,0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            
        </Grid>
    </Grid>

</UserControl>
